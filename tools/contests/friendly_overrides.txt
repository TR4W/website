# Manual friendly-name annotations for TR4W contests that are NOT in the
# WA7BNM cabnames table (so the join leaves them blank). Rendered as PLAIN TEXT
# (no link), in the Contest column, instead of the blank "—" sentinel.
#
# Format:  ContestName|Friendly text
#   - ContestName must match wa7bnm_contest_ids.csv exactly.
#   - Use this only for contests with no single WA7BNM entry. If a contest IS in
#     the WA7BNM table, give it the real ref id in the CSV instead (it auto-links).
#   - '#' lines are comments.

TEN TEN|Various Ten Tec Contests
WRTC|IARU Contest as a WRTC Competitor
IN7QPNE|Combined Indiana, 7th Area, New England and Delaware QSO Parties
